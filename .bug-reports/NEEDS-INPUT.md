# Needs your input - NarutoOverhaul
_Regenerated by the fixer routine on 2026-09-18. Items you have since resolved drop off automatically._

## Unverifiable in the cloud
The fixer has no test runner for this repo in the cloud environment (NarutoOverhaul needs a real
tModLoader + dotnet install to build/run, which is not available here). Fixes are found and
reviewed each run but always discarded unverified - this repo gets audit reports, not automatic
fixes. Options: add a headless/CLI test runner - apply the fixes below manually - drop this repo
from the fixer routine.

Findings from `.bug-reports/2026-09-18.md`, fixed locally this run and then discarded
(`git reset --hard`) because they could not be verified with a build:
- **GentleFistItem spends Stamina twice per swing because its UseAnimation override and the shared
  TaijutsuKickItemBase.UseAnimation it calls into both independently call TrySpendStamina** (high
  bug): `Content/Items/Weapons/Jutsu/GentleFistItem.cs:61-71`,
  `Content/Items/Weapons/Jutsu/TaijutsuKickItemBase.cs:44-56` - fix exposes the base's spend result
  as a `protected bool StaminaSpent { get; private set; }` property instead of a private field,
  removes `GentleFistItem`'s own redundant `TrySpendStamina` call and its now-unneeded
  `UseAnimation` override entirely (the base's `UseAnimation` already does everything it needs),
  and updates `GentleFistItem.ModifyHitNPC`/`OnHitNPC` to read the inherited `StaminaSpent`
  property instead of a separate local field. Took the "minimal diff" option the report flagged as
  needing a decision (protected property, not a new post-spend hook method) since it's a purely
  internal code-structure choice, not a product/design decision, and the routine's own default is
  smallest-diff-first.

## Unfixed high-severity bugs
### GentleFistItem spends Stamina twice per swing
- Report: .bug-reports/2026-09-18.md
- Location: Content/Items/Weapons/Jutsu/GentleFistItem.cs:61-71, Content/Items/Weapons/Jutsu/TaijutsuKickItemBase.cs:44-56
- Why unfixed: discarded-unverified (no build/test runner available in the cloud environment for this repo)
- What the fix would be: stop `GentleFistItem.UseAnimation` from spending stamina itself; let it rely entirely on the base's spend, reading the base's result via a new `protected StaminaSpent` property. See the "Unverifiable in the cloud" entry above for the fix already written and verified-by-review this run.

## Design decisions
None this run - the 2026-09-18 report's implementation-choice question (expose the base's spend
result via a protected property vs. a new post-spend hook method) was resolved in favor of the
smaller-diff protected-property option per the fixer routine's own preference; it's an internal
code-structure choice, not a genuine product/design decision.

## Public API / format / contract changes
None this run - no finding in the 2026-09-18 report touches a public API, on-disk save/data
format, or network contract. The new `TaijutsuKickItemBase.StaminaSpent` protected member is
internal to this mod's own class hierarchy, not a cross-mod API surface.
