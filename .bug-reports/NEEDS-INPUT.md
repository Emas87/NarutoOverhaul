# Needs your input - NarutoOverhaul
_Regenerated by the fixer routine on 2026-09-21. Items you have since resolved drop off automatically._

## Unverifiable in the cloud
The fixer has no test runner for this repo in the cloud environment (NarutoOverhaul needs a real
tModLoader + dotnet build to verify a change compiles and behaves correctly). Fixes are found and
reviewed each run but always discarded unverified - this repo gets audit reports, not automatic
fixes. Options: add a headless/CLI build step that can run in this environment - apply the fixes
below manually - drop this repo from the fixer routine.

## Unfixed high-severity bugs
### HiraishinMarkerSystem's new HandleAddMarkPacket trusts client-supplied tile coordinates with zero validation
- Report: .bug-reports/2026-09-21.md
- Location: Common/Systems/HiraishinMarkerSystem.cs:123-132
- Why unfixed: discarded-unverified
- What the fix would be: before calling `AddMark`, check `WorldGen.InWorld(x, y)` and that
  `Main.tile[x, y].HasTile && Main.tile[x, y].TileType == ModContent.TileType<HiraishinSealTile>()`,
  and silently drop the packet otherwise.

## Design decisions
None pending - both bugs in the 2026-09-21 report had a fix applied this run (then discarded
unverified, see above); no finding was skipped for needing a product/design decision.

## Public API / format / contract changes
None this run - none of this run's changes touched a save format, network packet shape, or public
API in a way callers would notice.
