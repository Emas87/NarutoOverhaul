# Needs your input - NarutoOverhaul
_Regenerated by /fix-unfixes on 2026-09-23. Items you have since resolved drop off automatically._

## Unverifiable in the cloud
The fixer has no test runner for this repo in the cloud environment (NarutoOverhaul needs a real
tModLoader + dotnet build to verify a change compiles and behaves correctly). Fixes are found and
reviewed each run but always discarded unverified - this repo gets audit reports, not automatic
fixes. Options: add a headless/CLI build step that can run in this environment - apply the fixes
below manually - drop this repo from the fixer routine.

## Unfixed high-severity bugs
None - the 2026-09-23 report found 0 new bugs.

## Design decisions
None pending - resolved and applied on 2026-09-23 (user picked option B): `ShadowCloneItem` now
caps concurrent clones per owner at `MaxConcurrentClones = 3` (`CountOwnedClones` scans
`Main.projectile` by owner/type, checked in both `CanUseItem` and `UseItem`). Also applied the
paired scan-throttling perf fix from the same finding: `ShadowCloneProjectile.FindTarget` now
re-scans `Main.maxNPCs` only every 10 ticks and coasts on a cached target in between (dropped
immediately if it's no longer chaseable), instead of a full unthrottled scan every tick per clone.

## Public API / format / contract changes
None this run - none of this run's findings touch a save format, network packet shape, or public
API in a way callers would notice.
