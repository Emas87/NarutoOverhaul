# Needs your input - NarutoOverhaul
_Regenerated by the fixer routine on 2026-09-13. Items you have since resolved drop off automatically._

## Unverifiable in the cloud
The fixer has no test runner for this repo in the cloud environment (no dotnet/tModLoader install available). Fixes are found and reviewed each run but always discarded unverified - this repo gets audit reports, not automatic fixes. Options: add a headless/CLI test runner - apply the fixes below manually - drop this repo from the fixer routine.

## Unfixed high-severity bugs
None this run - the newest report (2026-09-13) contains 0 high-severity bugs (1 medium, 1 low). For completeness, both are flagged anyway since they're straightforward, reviewed-and-ready fixes that just can't be verified in this environment:

### PainBoss's Preta Path chakra drain still has no multiplayer-client guard
- Report: .bug-reports/2026-09-13.md
- Location: Content/NPCs/Bosses/PainBoss.cs:560-568 (`DoPreta`)
- Severity: medium
- Why unfixed: discarded-unverified
- What the fix would be: wrap the `TrySpendChakra`/`SendCorrection` block in `if (Main.netMode != NetmodeID.MultiplayerClient) { ... }`, matching the guarded siblings `DoAnimal`/`DoAsura` in the same file; leave the `ChakraVFX.SpawnGenjutsuBurst` call outside the guard so the burst still plays for every client.

### StarterItemPlayer's debug item giveaway still skips the multiplayer-client guard its sibling methods use
- Report: .bug-reports/2026-09-13.md
- Location: Common/Players/StarterItemPlayer.cs:59-96 (`GiveStarterItemsOnce`)
- Severity: low
- Why unfixed: discarded-unverified
- What the fix would be: add `if (Main.netMode == NetmodeID.MultiplayerClient) return;` at the top of `GiveStarterItemsOnce`, matching `ForceTownNpcSpawn`/`ForceStoryProgressUnlock` in the same class.

## Design decisions

### Should the 4 Taijutsu/Genjutsu blast-VFX items' UseAnimation be folded into a shared base class?
- Report: .bug-reports/2026-09-13.md
- Location: Content/Items/Weapons/Jutsu/IronLegItem.cs:58-69, FrontLotusItem.cs:61-74, LeafHurricaneItem.cs:57-74, GentleFistItem.cs:58-74
- The decision: all four items repeat the same `UseAnimation` shape (impact flash + player-anchored blast + dash nudge), differing only in burst type/offsets/scale/dash speed. The report itself flags this as worth doing as its own deliberate pass rather than a drive-by, alongside the already-tracked `CanUseItem`/`Shoot`/tooltip duplication from `.bug-reports/2026-09-11.md`.
- Options: A) Build the previously-proposed shared `TaijutsuKickItem` base and fold this `UseAnimation` shape into it (e.g. a `virtual PlayVFXAndDash(Player)` driven by abstract `BlastProjectileType` + per-item offset/scale/dash speed), including `GentleFistItem` as a sibling even though it has no `Shoot` override. B) Leave the duplication as-is for now given there's no automated combat/VFX test coverage to verify a multi-file behavior-preserving refactor. C) Do a narrower refactor limited to just `UseAnimation` without touching the `Shoot`/tooltip duplication from the prior report.

## Public API / format / contract changes
None this run - no finding in the 2026-09-13 report touches a public API, on-disk format, or network contract.
