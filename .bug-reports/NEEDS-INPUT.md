# Needs your input - NarutoOverhaul
_Regenerated by the fixer routine on 2026-09-16. Items you have since resolved drop off automatically._

## Unverifiable in the cloud
The fixer has no test runner for this repo in the cloud environment (NarutoOverhaul needs a real
tModLoader + dotnet install to build/run, which is not available here). Fixes are found and
reviewed each run but always discarded unverified - this repo gets audit reports, not automatic
fixes. Options: add a headless/CLI test runner - apply the fixes below manually - drop this repo
from the fixer routine.

Findings from `.bug-reports/2026-09-16.md`, fixed locally this run and then discarded
(`git reset --hard`) because they could not be verified with a build:
- **TaijutsuKickItemBase.UseAnimation plays its dash + full-body VFX blast unconditionally, even
  when the paired Shoot() stamina spend fails** (medium bug):
  `Content/Items/Weapons/Jutsu/TaijutsuKickItemBase.cs:41-51` - fix spends stamina once in
  `UseAnimation`, stores the result in a `_staminaSpent` field, returns early on failure before the
  VFX/dash calls, and has `Shoot()` return that stored result instead of re-rolling a second
  independent `TrySpendStamina` call - matching the pattern `GentleFistItem` already uses.
- **HiraishinMarkerSystem.LoadWorldData assumes hiraishinMarkX and hiraishinMarkY are always the
  same length** (improvement): `Common/Systems/HiraishinMarkerSystem.cs:47-63` - fix clamps the
  load loop to `Math.Min(xs.Count, ys.Count)` instead of indexing both lists by `xs.Count`, so a
  mismatched-length save doesn't throw and fail world load entirely.

## Unfixed high-severity bugs
None this run - the newest report (2026-09-16) contains 0 high-severity bugs (1 medium, 0 low).

## Design decisions
None this run - the 2026-09-16 report's one open question (whether tModLoader guarantees
`CanUseItem` and `Shoot()` observe the same `Stamina` value in the same tick) doesn't block the fix
above, since the suggested fix is strictly safer regardless of the answer.

### VFX burst dispatch in ChakraVFX.cs is one hand-written wrapper method per effect type
- Report: .bug-reports/2026-09-08.md
- Location: Common/VFX/ChakraVFX.cs:56-71 (`SpawnChakraBurst` ... `SpawnImpactBurst`, 15 near-identical one-liners)
- The decision: whether to drop the 15 wrapper methods in favor of call sites using `ChakraVFX.SpawnBurstEffect<T>(...)` directly (making that method public), or to keep the named wrappers because they're more readable at call sites. Not a defect either way - purely a duplication-vs-readability tradeoff the report itself declines to resolve.
- Options: A) Remove the 15 wrappers, make `SpawnBurstEffect<T>` public, update every call site to the generic form. B) Keep the wrappers as-is; accept that a 16th effect family means a 16th copy-pasted one-liner. C) Keep the wrappers but add a comment at the top of the block noting the tradeoff was considered, so it isn't re-flagged as an oversight in future audits.

## Public API / format / contract changes
None this run - no finding in the 2026-09-16 report touches a public API, on-disk save/data format, or network
contract.
