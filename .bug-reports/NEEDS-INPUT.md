# Needs your input - NarutoOverhaul
_Regenerated by /fix-unfixes on 2026-09-14. Items you have since resolved drop off automatically._

## Resolved this run (2026-09-13 /fix-unfixes, verified via dotnet build)
- **PainBoss's Preta Path chakra drain missing multiplayer-client guard** (medium): `DoPreta` now
  wraps the `TrySpendChakra`/`SendCorrection` block in `if (Main.netMode != NetmodeID.MultiplayerClient)`,
  matching `DoAnimal`/`DoAsura`; `ChakraVFX.SpawnGenjutsuBurst` stays outside the guard so the
  burst still plays for every client.
- **StarterItemPlayer's debug item giveaway missing multiplayer-client guard** (low): `GiveStarterItemsOnce`
  now returns early on `Main.netMode == NetmodeID.MultiplayerClient`, matching `ForceTownNpcSpawn`/`ForceStoryProgressUnlock`.
- **Taijutsu/Genjutsu blast-VFX UseAnimation duplication** (design decision, resolved: build shared base):
  added `TaijutsuKickItemBase<TBlast>` (Content/Items/Weapons/Jutsu/TaijutsuKickItemBase.cs) with
  a `virtual PlayImpactFlash` + `PlayBlast` + `ApplyDash`, driven by abstract `StaminaCost`/`BlastOffset`/`OnUseDashSpeed`
  and virtual `ImpactBurstOffsetX`/`ImpactBurstScale`. IronLegItem/FrontLotusItem use the default
  single-flash shape; LeafHurricaneItem overrides `PlayImpactFlash` for its two-flash+dust sweep;
  GentleFistItem (no `Item.shoot`, so the base's `Shoot()` never fires for it) wraps its own
  stamina-gate around `base.UseAnimation(player)`.

Verified: `PATH="$HOME/.dotnet:$PATH" ~/.dotnet/dotnet build NarutoOverhaul.csproj -c Debug` -
Build succeeded, 0 warnings, 0 errors (nested tModLoader build step needed `~/.dotnet`'s .NET 8
ahead of the system's .NET 10 on PATH - the bare `dotnet tModLoader.dll -build` sub-invocation
otherwise fails with "You must install or update .NET to run this application").

Note: the 2026-09-14 cloud audit report re-flagged the PainBoss/StarterItemPlayer guards as
still-unfixed because this fix commit (`3d0abf5`) was local-only and hadn't reached
`origin/master` yet when that report ran against remote HEAD. The commit is now merged into
master via this run.

## Resolved this run (2026-09-14 /fix-unfixes, verified via dotnet build + manual test)
- **generate_armor_sheet.py silently clipped the bottom of a Head frame** (medium): when
  `anchor == "top"`, `_center_on_canvas` now scales against `canvas_h - HEAD_TOP_MARGIN` instead
  of the full canvas height, so a full-height Head frame fits below the 10px top margin instead
  of clipping at the bottom. Verified: simulated a full-height (40x56) frame through
  `_center_on_canvas(..., anchor="top")` - bbox bottom now stays within the 56px canvas and the
  top margin is respected (was clipping past the canvas edge before the fix).
- **extract_tmod.py never closed the `.tmod` file handle it opened** (improvement): `parse_tmod`
  now reads the file inside a `with` block and returns raw bytes instead of a live handle;
  `extract_file` slices the in-memory bytes instead of seeking a file object. Verified against a
  real installed mod (`NarutoOverhaul.tmod`, 207 files) - `list` and `extract` (decoding a
  `.rawimg` to `.png`) both still work correctly.

## Unfixed high-severity bugs
None this run.

## Design decisions
None this run - no finding needs a product/design decision.

## Public API / format / contract changes
None this run - `TaijutsuKickItemBase<TBlast>` is new internal scaffolding; `parse_tmod`'s return
shape changed (file handle -> raw bytes) but it's a repo-internal dev tool with one caller
(`main()` in the same file), not a public API.
