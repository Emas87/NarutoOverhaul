# Needs your input - NarutoOverhaul
_Regenerated by `/fix-unfixes` on 2026-09-11 (local session). Items you have since resolved drop off automatically._

## Resolved this run
The cloud fixer has no dotnet/tModLoader install and always discards fixes
here unverified. This local session found the real toolchain installed
(`/home/ema/.dotnet/dotnet` 8.0.423 + tModLoader under
`~/.steam/debian-installation/steamapps/common/tModLoader`), built the mod
(`dotnet build` - Build succeeded, 0 warnings/0 errors) after each change,
and applied:

- **Rasengan/Gentle Fist free-cast bug** (medium): `RasenganItem.Shoot()` now
  returns `TrySpendChakra`'s result directly. `GentleFistItem` captures
  `TrySpendStamina`'s result in a new `_staminaSpent` field, set in
  `UseAnimation` (which now bails out before any VFX/dash if spending
  failed) and checked in `ModifyHitNPC`/`OnHitNPC` before applying the
  armor-pen bonus / impact VFX.
- **`slice_fx_sheet.py` unclear crash** (low): `slice_sheet()` now validates
  `row_margin * 2 < src_row_h` right after computing `src_row_h` and raises
  a `ValueError` naming the offending rows/height; `main()` catches it and
  exits with a clean `error: ...` message instead of a raw traceback.
- **`extract_tmod.py` improvement**: `_read_7bit_string` now raises a clear
  `EOFError` (naming the offset) on truncated input instead of an opaque
  `IndexError: index 0 is out of range` from `f.read(1)[0]`.

## Deliberately skipped
- **Extract a shared `TaijutsuKickItem` base for `IronLegItem`/
  `FrontLotusItem`/`LeafHurricaneItem`**: the cloud report called this
  "trivial-risk," but reading all three shows each has distinct per-item
  dash speed, VFX offsets, and hitbox tuning with dense comments explaining
  *why* each value differs. A shared base is still a reasonable idea, but
  it's a real multi-file refactor with behavior-preservation risk in a
  content mod with no automated combat-balance tests - better done as its
  own deliberate pass than folded in here. Still open if you want it done.

## Design decisions
None this run.

## Public API / format / contract changes
None this run - all changes are internal to their own item classes / tools,
no save format, network contract, or public API touched.
