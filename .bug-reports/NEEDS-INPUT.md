# Needs your input - NarutoOverhaul
_Regenerated by /fix-unfixes on 2026-09-19. Items you have since resolved drop off automatically._

## Resolved this run (2026-09-19, via /fix-unfixes, verified via local `dotnet build` + `.tmod` decode)
- **GentleFistItem spends Stamina twice per swing** (high bug, carried over from 2026-09-18): fixed
  per the report's minimal-diff option - `TaijutsuKickItemBase` now exposes `StaminaSpent` as a
  `protected bool { get; private set; }` property instead of a private field, `GentleFistItem`'s
  own redundant `TrySpendStamina` call was removed from its `UseAnimation` override (which is now
  gone entirely, relying on the inherited one), and `ModifyHitNPC`/`OnHitNPC` read the inherited
  property.
- **GenjutsuNightmareItem plays its cast VFX even when the Chakra spend fails** (medium bug): fixed
  - the VFX calls in `UseAnimation` now sit behind an early `return` when `TrySpendChakra` fails,
  matching `TaijutsuKickItemBase.UseAnimation`'s pattern.
- **Hiraishin seals planted by a non-host client never register with the server's authoritative
  mark registry** (design decision, resolved: option A - dedicated packet): added
  `NetMessageType.HiraishinAddMark`, `HiraishinMarkerSystem.SendAddMarkRequest`/`HandleAddMarkPacket`,
  and wired `MinatoKunaiProjectile.PlantSeal` to send the request alongside its existing
  `SendTileSquare` call when a non-host client plants a seal - server now calls `AddMark` itself
  instead of relying on `PlaceInWorld`, which only ever ran on the placing client.

Verified: `~/.dotnet/dotnet build` (with `DOTNET_ROOT`/`PATH` pointed at `~/.dotnet`) - 0 errors, 0
warnings. Packaged `.tmod` at `~/.local/share/Terraria/tModLoader/Mods/NarutoOverhaul.tmod` decoded
with `tools/extract_tmod.py` to confirm the freshly-built `NarutoOverhaul.dll` is present.

## Unfixed high-severity bugs
None - both bugs from the 2026-09-19 report were fixed and verified above.

## Design decisions
None pending - the Hiraishin design decision above was resolved this run.

## Public API / format / contract changes
None this run - the Stamina-spend fixes are internal to this mod's class hierarchy. The new
`HiraishinAddMark` packet is an internal mod-to-mod network message (not a save/world format
change); `MarkedTiles`' persisted save shape is unchanged.
