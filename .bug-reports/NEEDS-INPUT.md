# Needs your input - NarutoOverhaul
_Regenerated by /fix-unfixes on 2026-09-20. Items you have since resolved drop off automatically._

## Resolved this run (2026-09-20, via /fix-unfixes, verified via local `dotnet build` + `.tmod` decode)
- **TailedBeastBoss silently self-deactivated the instant its current target died, instead of
  following every sibling boss's despawn pattern** (high bug, design decision resolved: trust the
  sibling pattern): swapped `NPC.active = false; NPC.netUpdate = true;` for the same
  `NPC.velocity.Y -= 0.2f; NPC.EncourageDespawn(10);` pattern `HakuBoss`/`KakuzuBoss`/`MadaraBoss`/
  `OrochimaruBoss` already use for the identical no-target scenario. The removed comment's claim
  that `EncourageDespawn` doesn't work here couldn't be verified without a live multiplayer session,
  but the sibling bosses use it for the exact same case without reported issues, so the inconsistency
  was more likely stale/incorrect reasoning than a real exception - if it resurfaces as an actual
  bug, it'll now be consistent with (and as fixable as) the sibling bosses' shared pattern.
- **`ApplyGroundedMovement` rescanned up to 120 tiles downward every single tick even when the boss's
  horizontal position hadn't changed** (improvement): added a `_cachedCenterTileX`/`_cachedGroundTileY`
  cache, reused whenever `centerTileX` matches and the last scan actually found ground. A "no ground
  found" (-1) result is never cached/reused, since the boss is still falling in that case and
  `topTileY` keeps changing every tick even during a straight vertical fall where `centerTileX`
  stays fixed - caching that result would have made it fall through the world forever.

Verified: `~/.dotnet/dotnet build` (with `DOTNET_ROOT`/`PATH` pointed at `~/.dotnet`) - 0 errors, 0
warnings. Packaged `.tmod` at `~/.local/share/Terraria/tModLoader/Mods/NarutoOverhaul.tmod` decoded
with `tools/extract_tmod.py` to confirm the freshly-built `NarutoOverhaul.dll` (size changed,
matching the new code) is present.

## Unfixed high-severity bugs
None - the one bug from the 2026-09-20 report was fixed and verified above.

## Design decisions
None pending - the `EncourageDespawn` question above was resolved this run.

## Public API / format / contract changes
None this run - both changes are internal to `TailedBeastBoss`'s own AI and don't touch any save
format, network packet, or public API.
