# Needs your input - NarutoOverhaul
_Regenerated by the fixer routine on 2026-09-08. Items you have since resolved drop off automatically._

## Unverifiable in the cloud
The fixer has no test runner for this repo in the cloud environment (no dotnet/tModLoader install available). Fixes are found and reviewed each run but always discarded unverified - this repo gets audit reports, not automatic fixes. Options: add a headless/CLI test runner - apply the fixes below manually - drop this repo from the fixer routine.

## Unfixed high-severity bugs
None this run - the newest report (2026-09-08) contains 0 high-severity bugs (1 medium, 2 low). For completeness, the medium-severity bug is worth flagging anyway since it's a real multiplayer desync:

### PainBoss's Preta Path chakra drain is missing its multiplayer-client guard
- Report: .bug-reports/2026-09-08.md
- Location: Content/NPCs/Bosses/PainBoss.cs:566 (`DoPreta`)
- Severity: medium (not high, listed here because it's the report's most significant finding)
- Why unfixed: discarded-unverified
- What the fix would be: wrap the `TrySpendChakra(8f)` call (and the paired `ChakraPlayer.SendCorrection` call) in `if (Main.netMode != NetmodeID.MultiplayerClient) { ... }`, matching the guard already used by `DoAnimal`/`DoAsura` in the same file.

## Design decisions

### VFX burst dispatch in ChakraVFX.cs is one hand-written wrapper method per effect type
- Report: .bug-reports/2026-09-08.md
- Location: Common/VFX/ChakraVFX.cs:56-71 (`SpawnChakraBurst` ... `SpawnImpactBurst`, 15 near-identical one-liners)
- The decision: whether to drop the 15 wrapper methods in favor of call sites using `ChakraVFX.SpawnBurstEffect<T>(...)` directly (making that method public), or to keep the named wrappers because they're more readable at call sites. Not a defect either way - purely a duplication-vs-readability tradeoff the report itself declines to resolve.
- Options: A) Remove the 15 wrappers, make `SpawnBurstEffect<T>` public, update every call site to the generic form. B) Keep the wrappers as-is; accept that a 16th effect family means a 16th copy-pasted one-liner. C) Keep the wrappers but add a comment at the top of the block noting the tradeoff was considered, so it isn't re-flagged as an oversight in future audits.

## Public API / format / contract changes
None this run - no finding in the 2026-09-08 report touches a public API, on-disk save/data format, or network contract.
