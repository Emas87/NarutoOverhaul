# Needs your input - NarutoOverhaul
_Regenerated by the fixer routine on 2026-09-15. Items you have since resolved drop off automatically._

## Unverifiable in the cloud
The fixer has no test runner for this repo in the cloud environment (NarutoOverhaul needs a real
tModLoader + dotnet install to build/run, which is not available here). Fixes are found and
reviewed each run but always discarded unverified - this repo gets audit reports, not automatic
fixes. Options: add a headless/CLI test runner - apply the fixes below manually - drop this repo
from the fixer routine.

Findings from `.bug-reports/2026-09-15.md`, fixed locally this run and then discarded
(`git reset --hard`) because they could not be verified with a build:
- **slice_fx_sheet.py silently drops content from the last VFX frame when sheet height isn't an
  exact multiple of `--rows`** (medium bug): `tools/slice_fx_sheet.py:85,94-99` - fix would compute
  each row's `y0`/`y1` from `round(i * sheet.height / rows)` / `round((i + 1) * sheet.height / rows)`
  instead of floor-divided `src_row_h`, so the last row's true proportional slice (including any
  remainder pixels) is no longer silently truncated.
- **pixellab_generate.py's job-polling loop has no timeout or max-retry cap** (improvement):
  `tools/pixellab_generate.py:92-106` - fix would track elapsed time and raise a `RuntimeError` past
  a 15-minute ceiling so a stuck PixelLab job fails loudly instead of polling forever.
- **ChakraPlayer's SendCorrection comment states an assumption this codebase already proved false**
  (improvement): `Common/Players/ChakraPlayer.cs:168-171` - fix would reword the comment to describe
  the actual invariant (`DoPreta` now guards the spend itself; `SendCorrection` pushes the
  authoritative value to the drained player's own client) instead of the stale "AI() is server-only"
  reasoning.

## Unfixed high-severity bugs
None this run - the newest report contains no high-severity bugs.

## Design decisions
None this run - no finding needs a product/design decision.

## Public API / format / contract changes
None this run - no finding was skipped for changing a public API, on-disk format, or network
contract.
