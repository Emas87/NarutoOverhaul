# Needs your input - NarutoOverhaul
_Regenerated by the fixer routine on 2026-09-11. Items you have since resolved drop off automatically._

## Unverifiable in the cloud
The fixer has no test runner for this repo in the cloud environment (no dotnet/tModLoader install available). Fixes are found and reviewed each run but always discarded unverified - this repo gets audit reports, not automatic fixes. Options: add a headless/CLI test runner - apply the fixes below manually - drop this repo from the fixer routine.

## Unfixed high-severity bugs
None this run - the newest report (2026-09-11) contains 0 high-severity bugs (1 medium, 1 low). Flagging both non-high findings for completeness since this repo has no way to verify fixes in the cloud environment:

### Rasengan and Gentle Fist can fire/land for free because their resource-spend's success is never checked
- Report: .bug-reports/2026-09-11.md
- Location: Content/Items/Weapons/Jutsu/RasenganItem.cs:40-44 (`Shoot`), Content/Items/Weapons/Jutsu/GentleFistItem.cs:53-76 (`UseAnimation`/`ModifyHitNPC`/`OnHitNPC`)
- Severity: medium
- Why unfixed: discarded-unverified
- What the fix would be: gate `RasenganItem.Shoot()`'s return on `TrySpendChakra`'s result (either `return player.GetModPlayer<ChakraPlayer>().TrySpendChakra(ChakraCost);`, or capture the bool and skip the payoff the way `GenjutsuNightmareItem` does with its `chakraSpent` flag). For `GentleFistItem`, capture `TrySpendStamina`'s return in a field and gate `ModifyHitNPC`/`OnHitNPC`'s bonuses on it.

### slice_fx_sheet.py crashes with a raw ValueError instead of a clear error when `row_margin` doesn't fit the row height
- Report: .bug-reports/2026-09-11.md
- Location: tools/slice_fx_sheet.py:75-84 (`slice_sheet`), :95 (`--row-margin` arg, unvalidated)
- Severity: low
- Why unfixed: discarded-unverified
- What the fix would be: after computing `src_row_h`, validate `row_margin * 2 < src_row_h` and exit with a clear message (naming the offending rows/height) before attempting the crop.

The report's two improvements (extracting a shared `TaijutsuKickItem` base for `IronLegItem`/`FrontLotusItem`/`LeafHurricaneItem`; a clearer EOF error in `extract_tmod.py`'s `_read_7bit_string`) were also reviewed and would be trivial-risk mechanical changes, but are likewise discarded-unverified for the same reason - no runnable suite in this environment.

## Design decisions
None this run - no finding in the 2026-09-11 report needs a genuine product/design choice.

## Public API / format / contract changes
None this run - no finding in the 2026-09-11 report touches a public API, on-disk save/data format, or network contract.
