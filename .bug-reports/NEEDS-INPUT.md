# Needs your input - NarutoOverhaul
_Regenerated by the fixer routine on 2026-09-14. Items you have since resolved drop off automatically._

## Unverifiable in the cloud
The fixer has no test runner for this repo in the cloud environment (no dotnet/tModLoader install available). Fixes are found and reviewed each run but always discarded unverified - this repo gets audit reports, not automatic fixes. Options: add a headless/CLI test runner - apply the fixes below manually - drop this repo from the fixer routine.

## Unfixed high-severity bugs
None this run - the newest report (2026-09-14) contains 0 high-severity bugs (2 medium, 1 low), plus 1 improvement. All are flagged anyway for completeness since they're straightforward, reviewed-and-ready fixes that just can't be verified in this environment:

### generate_armor_sheet.py can still silently clip the bottom of a Head frame
- Report: .bug-reports/2026-09-14.md
- Location: tools/generate_armor_sheet.py:162 (`HEAD_TOP_MARGIN`), :165-176 (`_center_on_canvas`), :213 (`build`)
- Severity: medium
- Why unfixed: discarded-unverified
- What the fix would be: when `anchor == "top"`, compute `scale` against `canvas_h - HEAD_TOP_MARGIN` (46) instead of the full `canvas_h` (56), so a full-height Head frame scales down enough to fit below the 10px top margin instead of silently clipping at the bottom.

### PainBoss's Preta Path chakra drain still has no multiplayer-client guard
- Report: .bug-reports/2026-09-14.md
- Location: Content/NPCs/Bosses/PainBoss.cs:560-568 (`DoPreta`)
- Severity: medium
- Why unfixed: discarded-unverified
- What the fix would be: wrap the `TrySpendChakra`/`SendCorrection` block in `if (Main.netMode != NetmodeID.MultiplayerClient) { ... }`, matching the guarded siblings `DoAnimal`/`DoAsura` in the same file; leave the `ChakraVFX.SpawnGenjutsuBurst` call outside the guard so the burst still plays for every client.

### StarterItemPlayer's debug item giveaway still skips the multiplayer-client guard its sibling methods use
- Report: .bug-reports/2026-09-14.md
- Location: Common/Players/StarterItemPlayer.cs:59-96 (`GiveStarterItemsOnce`)
- Severity: low
- Why unfixed: discarded-unverified
- What the fix would be: add `if (Main.netMode == NetmodeID.MultiplayerClient) return;` at the top of `GiveStarterItemsOnce`, matching `ForceTownNpcSpawn`/`ForceStoryProgressUnlock` in the same class.

### extract_tmod.py still never closes the `.tmod` file handle it opens (improvement)
- Report: .bug-reports/2026-09-14.md
- Location: tools/extract_tmod.py:53-71 (`parse_tmod`), used in `main()` at :99-123
- Severity: improvement (robustness)
- Why unfixed: discarded-unverified
- What the fix would be: use `with open(path, "rb") as f:` in `parse_tmod` (returning read bytes/entries instead of a live handle), or at minimum add `f.close()` at the end of both branches in `main()`.

## Design decisions
None this run - no finding in the 2026-09-14 report needs a product/design decision.

## Public API / format / contract changes
None this run - no finding in the 2026-09-14 report touches a public API, on-disk format, or network contract.
